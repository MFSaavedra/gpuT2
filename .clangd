# Include paths and most flags now come from compile_commands.json (generated by
# `cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON`, symlinked to the repo root). This
# only pins the C++ standard as a fallback for files not yet in the compile DB.
CompileFlags:
  Add: [-std=gnu++23]
